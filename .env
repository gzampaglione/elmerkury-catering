# Keep the webpack runtime in an external file rather than an inline <script>.
# This lets the Content-Security-Policy in vercel.json use script-src 'self'
# without needing 'unsafe-inline'.
INLINE_RUNTIME_CHUNK=false
